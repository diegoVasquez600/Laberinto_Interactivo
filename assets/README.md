Coloca aquí la imagen de tablero con este nombre exacto:

- maze-board.png

El juego la cargará automáticamente como fondo del laberinto en `index.html`.
Si no existe ese archivo, usará el tablero dibujado por código.